@{
    ViewData["Title"] = "Solicitud enviada";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">Revisa tu correo</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success" role="alert">
                        @(ViewBag.SuccessMessage ?? "Si el usuario proporcionado existe, hemos enviado instrucciones para restablecer tu contraseña.")
                    </div>
                    <p class="mb-4">Sigue el enlace enviado a tu correo electrónico para completar el restablecimiento de tu contraseña.</p>

                    <div class="d-flex flex-column flex-sm-row gap-3">
                        <a asp-action="Login" class="btn btn-primary"><i class="fas fa-sign-in-alt me-1"></i> Volver al inicio de sesión</a>
                        <a asp-action="ForgotPassword" class="btn btn-outline-secondary"><i class="fas fa-redo me-1"></i> Enviar nuevamente</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
